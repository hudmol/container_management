<%
   if params['collection'].blank?
     selected_json = "{}"
   else
     selected_json = params['collection']['_resolved']
   end

   linkable_types = ["resource", "accession"]

%>

<div class="input-group linker-wrapper">
  <input type="text" class="linker"
         id="collection"
         data-label="<%= I18n.t("top_container._frontend.bulk_operations.collection_singular") %>"
         data-label_plural="<%= I18n.t("top_container._frontend.bulk_operations.collection_plural") %>"
         data-name="ref"
         data-path="collection"
         data-url="<%= url_for  :controller => :search, :action => :do_search, :format => :json %>"
         data-browse-url="<%= url_for :controller => :search, :action => :do_search, :format => :js, :facets => [], :sort => "title_sort asc" %>"
         data-selected="<%= selected_json %>"
         data-multiplicity="one"
         data-types='<%= linkable_types.to_json %>'
  />
  <div class="input-group-btn">
    <a class="btn btn-default dropdown-toggle last" data-toggle="dropdown" href="javascript:void(0);"><span class="caret"></span></a>
    <ul class="dropdown-menu">
      <li><a href="javascript:void(0);" class="linker-browse-btn"><%= I18n.t("actions.browse") %></a></li>
    </ul>
  </div>
</div>
